services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - mongodb+srv://manishkumar8882850061_db_user:jBBRhzQjNhMkeS0c@cluster0.tpykn8l.mongodb.net/?appName=Cluster0
      - MINIO_ENDPOINT=minio:9000
      - ELEVENLABS_API_KEY=${sk_0cd0e8d65c2d57a9b58259e4f5c10ee0801e6bc086e702a5}
    depends_on:
      - minio

  minio:
    image: minio/minio
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=manish
      - MINIO_ROOT_PASSWORD=StrongPassword123

volumes:
  minio_data: